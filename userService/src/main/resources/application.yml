spring:
  application:
    name: userService

  datasource:
    url: jdbc:mariadb://localhost:3306/Fitness
    username: root
    password: Naresh

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true