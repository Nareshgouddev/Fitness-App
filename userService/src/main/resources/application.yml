spring:
  application:
    name: userService

  datasource:
    url: jdbc:mariadb://localhost:3306/Fitness
    username: Naresh
    password: Naresh@034

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true